import React from "react";
import PropTypes from "prop-types";

const PrimaryButton = ({name, label, onClick, }) => {
    return ( 
        <button className=""
        >

        </button>
    );
}
 
    export default PrimaryButton;